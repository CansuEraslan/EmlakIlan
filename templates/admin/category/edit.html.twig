{% extends 'adminbase.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <section id="main-content">
        <section class="wrapper site-min-height">
            <h3><i class="fa fa-angle-right"></i> Edit Category Page</h3>
            <div class="row mt">
                <div class="col-lg-12">
                    <h1>Edit Category</h1>

                    {{ include('admin/category/_form.html.twig', {'button_label': 'Update'}) }}
                    <div class="btn-group">

                        <a href="{{ path('category_index') }}"><button type="button" class="btn btn-theme02">Back to list</button></a>
                    </div>
                    {{ include('admin/category/_delete_form.html.twig') }}
                </div>
            </div>
        </section>
        <!-- /wrapper -->
    </section>
    </div>
{% endblock %}
